#include <stdio.h>

int maxNum (int n) {
    int max = 
}

int main()
{
    int n;
    scanf("%d", &n);
    int a[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    
    maxNum(n);

    return 0;
}